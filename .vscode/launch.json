{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build & Debug MPIC",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/mpic_debug.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "expressions": "native",
            "preLaunchTask": "Make Build"
        }
    ]
}